services:
  medsenseclient:
    build: .
    container_name: medsenseclient-container
    ports:
      - "3029:3029"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    restart: unless-stopped